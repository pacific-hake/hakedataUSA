#' @param yvar A character value representing the column name
#' in `data` that is the dependent variable. There can only
#' be one entry in this argument, not a vector, and it must
#' match an existing column exactly.
